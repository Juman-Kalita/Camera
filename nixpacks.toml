[phases.setup]
nixPkgs = ["python312", "libGL", "glib", "libgcc"]

[phases.install]
cmds = ["pip install -r requirements-deploy.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
